local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({
    Name = "Asta Hub",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "MeuConfig"
})

-- Adicionando a aba "Player Controls"
local PlayerControlsTab = Window:MakeTab({
    Name = "Player Controls",
    Icon = "rbxassetid://4483345998",
    PremiumOnly = false
})

-- Adicionando seção "Controle de Velocidade" à aba "Player Controls"
local SpeedSection = PlayerControlsTab:AddSection({
    Name = "Speed Control"
})

-- Adicionando seção "Controle de Pulo" à aba "Player Controls"
local JumpSection = PlayerControlsTab:AddSection({
    Name = "Jump Control"
})

-- Adicionando seção "Teleporte" à aba "Player Controls"
local TeleportSection = PlayerControlsTab:AddSection({
    Name = "Teleport"
})

-- Adicionando seção "Noclip" à aba "Player Controls"
local NoclipSection = PlayerControlsTab:AddSection({
    Name = "Noclip"
})

-- Variável para armazenar o valor da velocidade
local speedValue = 16  -- Valor padrão da velocidade
local speedEnabled = false  -- Estado do hack de velocidade

-- Variável para armazenar o valor do pulo
local jumpPowerValue = 50  -- Valor padrão do pulo
local jumpEnabled = false  -- Estado do hack de pulo

-- Variáveis para teleporte
local savedPosition = nil  -- Armazena o ponto de retorno salvo
local noclipEnabled = false  -- Estado do noclip

-- Função para atualizar a velocidade do jogador
local function updateSpeed(value)
    local character = game.Players.LocalPlayer.Character
    if character and character:FindFirstChild("Humanoid") then
        character.Humanoid.WalkSpeed = value
    end
end

-- Função para atualizar o pulo do jogador
local function updateJumpPower(value)
    local character = game.Players.LocalPlayer.Character
    if character and character:FindFirstChild("Humanoid") then
        character.Humanoid.UseJumpPower = true  -- Garantir que UseJumpPower esteja ativado
        character.Humanoid.JumpPower = value
    end
end

-- Função para ativar/desativar noclip
local function toggleNoclip()
    noclipEnabled = not noclipEnabled
    local character = game.Players.LocalPlayer.Character

    if character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = not noclipEnabled
            end
        end
    end

    if noclipEnabled then
        game:GetService("RunService").Stepped:Connect(function()
            if noclipEnabled and character then
                for _, part in pairs(character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    end

    print(noclipEnabled and "Noclip Ativado" or "Noclip Desativado")
    OrionLib:MakeNotification({
        Name = "Noclip",
        Content = noclipEnabled and "Noclip Ativado" or "Noclip Desativado",
        Image = "rbxassetid://4483345998",
        Time = 5
    })
end

-- Função para salvar o ponto de retorno
local function saveReturnPoint()
    local character = game.Players.LocalPlayer.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        savedPosition = character.HumanoidRootPart.CFrame
        OrionLib:MakeNotification({
            Name = "Ponto Salvo",
            Content = "Ponto de retorno salvo com sucesso!",
            Image = "rbxassetid://4483345998",
            Time = 5
        })
    end
end

-- Função para retornar ao ponto salvo
local function returnToSavedPoint()
    local character = game.Players.LocalPlayer.Character
    if savedPosition and character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = savedPosition
        OrionLib:MakeNotification({
            Name = "Teleporte",
            Content = "Teletransportado para o ponto salvo!",
            Image = "rbxassetid://4483345998",
            Time = 5
        })
    else
        OrionLib:MakeNotification({
            Name = "Erro",
            Content = "Nenhum ponto salvo encontrado.",
            Image = "rbxassetid://4483345998",
            Time = 5
        })
    end
end

-- Adicionando um botão para ativar/desativar o "hack de velocidade" na seção de velocidade
SpeedSection:AddButton({
    Name = "Velocidade",
    Callback = function()
        speedEnabled = not speedEnabled
        if speedEnabled then
            speedValue = 50
            updateSpeed(speedValue)
            print("Speed Hack Ativado")
        else
            speedValue = 16
            updateSpeed(speedValue)
            print("Speed Hack Desativado")
        end
        updateStatus(SpeedSection, speedEnabled, "Velocidade: " .. (speedEnabled and "Ativado" or "Desativado"))
    end
})

-- Adicionando uma caixa de texto para ajustar a velocidade na seção de velocidade
SpeedSection:AddTextbox({
    Name = "Definir velocidade",
    Default = tostring(speedValue),
    TextDisappear = false,  -- Mantém o valor na caixa de texto
    Callback = function(value)
        local newValue = tonumber(value)
        if newValue and newValue >= 0 e newValue <= 99999 then
            speedValue = newValue
            if speedEnabled then
                updateSpeed(speedValue)
            end
        else
            OrionLib:MakeNotification({
                Name = "Erro",
                Content = "Por favor, insira um valor entre 0 e 99999 para a velocidade.",
                Image = "rbxassetid://4483345998",
                Time = 5
            })
        end
    end,
    Width = 200  -- Ajusta a largura da caixa de texto
})

-- Adicionando um botão para ativar/desativar o "hack de pulo" na seção de pulo
JumpSection:AddButton({
    Name = "Pulo",
    Callback = function()
        jumpEnabled = not jumpEnabled
        if jumpEnabled then
            jumpPowerValue = 100
            updateJumpPower(jumpPowerValue)
            print("Hack de Pulo Ativado")
        else
            jumpPowerValue = 50
            updateJumpPower(jumpPowerValue)
            print("Hack de Pulo Desativado")
        end
        updateStatus(JumpSection, jumpEnabled, "Pulo: " .. (jumpEnabled and "Ativado" or "Desativado"))
    end
})

-- Adicionando uma caixa de texto para ajustar a altura do pulo na seção de pulo
JumpSection:AddTextbox({
    Name = "Definir pulo",
    Default = tostring(jumpPowerValue),
    TextDisappear = false,  -- Mantém o valor na caixa de texto
    Callback = function(value)
        local newValue = tonumber(value)
        if newValue and newValue >= 0 e newValue <= 99999 then
            jumpPowerValue = newValue
            if jumpEnabled then
                updateJumpPower(jumpPowerValue)
            end
        else
            OrionLib:MakeNotification({
                Name = "Erro",
                Content = "Por favor, insira um valor entre 0 e 99999 para o pulo.",
                Image = "rbxassetid://4483345998",
                Time = 5
            })
        end
    end,
    Width = 200  -- Ajusta a largura da caixa de texto
})

-- Adicionando uma lista suspensa (dropdown) para selecionar o jogador na seção de teleporte
TeleportSection:AddDropdown({
    Name = "Selecionar Jogador",
    Options = updatePlayerList(),  -- Atualiza a lista de jogadores
    Callback = function(selectedName)
        if selectedName then
            teleportToPlayer(selectedName)
        end
    end
})

-- Adicionando botão para salvar o ponto de retorno
TeleportSection:AddButton({
    Name = "Salvar Ponto de Retorno",
    Callback = saveReturnPoint
})

-- Adicionando botão para retornar ao ponto salvo
TeleportSection:AddButton({
    Name = "Voltar ao Ponto Salvo",
    Callback = returnToSavedPoint
})

-- Adicionando botão para ativar/desativar noclip
NoclipSection:AddButton({
    Name = "Noclip",
    Callback = toggleNoclip
})

-- Atualiza a lista de jogadores periodicamente
game.Players.PlayerAdded:Connect(function()
    local dropdown = TeleportSection:GetDropdown("Selecionar Jogador")
    dropdown:SetOptions(updatePlayerList())
end)

game.Players.PlayerRemoving:Connect(function()
    local dropdown = TeleportSection:GetDropdown("Selecionar Jogador")
    dropdown:SetOptions(updatePlayerList())
end)

-- Notificação de boas-vindas
OrionLib:MakeNotification({
    Name = "Bem-vindo!",
    Content = "Você entrou no hub atualizado com novas funcionalidades!",
    Image = "rbxassetid://1234567890",  -- Atualize com o ID da sua imagem
    Time = 10
})
